export function soma(a, b) {
  return a + b;
}

export const dois = 2;

// podemos importar e exportar funções e variáveis utilizando o javacsript
// quando temos apenas uma função, podemos exporta-la com o export default,
// e importa-la sem as chaves, além de também podermos renomeá-las
